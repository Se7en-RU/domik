// Registration identifies a room; it does not certify the dimensions.
export function renderWholeHouse(report, spec) {
  const cm = (v) => (100 * v).toFixed(1);
  const fit = report.coupledFit;
  const wall = spec.walls.find((w) => w.id === "F2-W15");
  const room = spec.rooms.find((r) => r.id === "F2-ROOM");
  const rows = report.scans.map((s) => {
    const spans = s.comparisons.filter((c) => c.field.startsWith("room-face-span:"));
    return `| ${s.file} | ${s.registration.candidateId ?? "—"} | ${s.registration.status} | ${s.walls?.filter((w) => w.status === "MATCHED").length ?? 0}/${s.counts?.walls ?? 0} | ${spans.length ? cm(Math.max(...spans.map((c) => Math.abs(c.delta)))) : "—"} |`;
  });
  const important = report.comparisons.filter(
    (c) => c.observations > 0 && Math.abs(c.delta) > 0.03,
  );
  const covered = new Set(
    report.scans
      .filter((s) => s.registration.status === "MATCHED")
      .flatMap((s) => s.registration.roomIds),
  );
  return `# Согласование дома с RoomPlan · 09.09.2026

Ревизия ${spec.revision}. Проверены все ${report.scans.length} JSON из measurements/raw. Исходные сканы не изменялись. Повторная проверка: npm run measurements:audit.

${fit ? "По разрешению владельца выполнена совместная подгонка размеров. Размеры округлены до 5 мм; оси стен нечётной толщины могут иметь шаг 2,5 мм. Общие стены, комнаты, проёмы, плиты и скосы используют согласованные координаты." : "Совместная подгонка ещё не выполнена."}

## Применённые изменения

- Внутренняя ширина дома: ${spec.parameters.upperMainWidth ?? 8.82} м; разделяющие стены подобраны совместно по комнатам обоих этажей.
- Угол F2-W15/F2-W06: видимая грань ${(wall.b[1] - room.polygon[0][1]).toFixed(3)} м против 0,875 м в скане; глубина уступа ${(room.polygon[3][0] - room.polygon[2][0]).toFixed(3)} м против 0,194 м. Толщина F2-W06 ${spec.walls.find((w) => w.id === "F2-W06").t} м выведена из положения обеих граней по соседним комнатам. Это расчётная привязка, не прямой ручной замер толщины.
- Уточнены кабинет, бойлерная, оба санузла, детская, спальня, гардеробная, комната и коридор; соседние полигоны и подписи согласованы. Неподтверждённые двери сохраняют прежний размер и привязаны к своим стенам.
- Проёмы сопоставлены по родительской стене и положению. Ошибочная классификация остеклённого проёма как двери не меняет его тип в модели. Парные окна F1-WIN01/F1-WIN02 приведены к общему типу 2,440 × 2,615 м (ширины сканов 2,410 и 2,470 м). Остальные однозначные ширины, высоты и низы взяты из сканов с округлением.
- Ранее подтверждённая группа высот F1-WIN04/F1-WIN05/F2-WIN04 = 1,400 м сохранена. Эти проёмы имеют заметный разброс высот в сканах.
- Гараж перенесён целиком на X=${spec.garage.fitTranslation?.x ?? 0}, Z=${spec.garage.fitTranslation?.z ?? 0} м вслед за общей стеной. Ширина 6,361 м, боковые стены 6,376/7,131 м, ворота 4,743 м и исходная форма сохранены без дополнительного округления; выступ выровнен с домом.
- Низкие стены скосов: ${spec.parameters.leftKneeHeight}/${spec.parameters.rightKneeHeight}/${spec.parameters.southRightKneeHeight} м (слева/справа/нижняя комната). Высоты 3,030/3,045/2,927 м и ручные толщины 0,415/0,405/0,130 м сохранены. Линии скатов пересчитаны с прежним уклоном, округлённым через длины скатов.

## Остатки совместного подбора

${
  fit
    ? `Из ${fit.equations.length} уравнений ${fit.equations.filter((e) => Math.abs(e.residual) <= 0.03).length} имеют остаток не более 3 см. Максимум ${cm(Math.max(...fit.equations.map((e) => Math.abs(e.residual))))} см. В таблице величины приведены к общим опорным линиям с учётом сохранённых толщин стен; это не всегда чистая ширина комнаты.

| Источник / поверхность | Цель между опорными линиями, м | Модель, м | Остаток, см |
|---|---:|---:|---:|
${fit.equations.map((e) => `| ${e.source} | ${e.value.toFixed(3)} | ${e.fitted.toFixed(3)} | ${cm(e.residual)} |`).join("\n")}`
    : "Нет данных подбора."
}

Зеркальная/развёрнутая ориентация прямоугольного санузла не влияет на его ширину и глубину. Поэтому размеры этих помещений использованы в совместном подборе, хотя автоматическая регистрация оставляет REVIEW REQUIRED. Для бойлерной использован связный Г-образный контур; случайные продолжения за пределы помещения исключены. Повтор одной грани в одном скане не считается независимой съёмкой (две оценки глубины бойлерной получили суммарный вес одной).

## Повторная автоматическая сверка

| Файл | Гипотеза | Регистрация | Сопоставлено стен | Макс. Δ сравнимых граней, см |
|---|---|---|---:|---:|
${rows.join("\n")}

MATCHED — гипотеза помещения и ориентации, не метрологическая гарантия. Максимумы относятся к целым сопоставленным граням, а не ко всем поверхностям. Неполные стены и откосы остаются в audit.json и overlay.html. Помещения без однозначной автоматической регистрации: ${spec.rooms
    .filter((r) => !covered.has(r.id))
    .map((r) => r.id)
    .join(
      ", ",
    )}. Отдельного полного скана прихожей нет; её контур следует соседним стенам. Лестница.json содержит смешанные высоты двух этажей и не подгоняет их независимо.

## Оставшиеся расхождения >3 см в однозначных сопоставлениях

| Объект | Поле | Модель, м | Скан, м | Δ, см |
|---|---|---:|---:|---:|
${important.map((c) => `| ${c.target} | ${c.field} | ${c.model} | ${c.measured} | ${cm(c.delta)} |`).join("\n")}

Лестничное окно F2-WIN04 имеет противоречивые позиции и низы в сканах комнаты, коридора и лестницы. Его координаты и подтверждённая высота сохранены; перевод относительных высот разных сканов без общей высотной опоры небезопасен. Расхождение спальни около 3,5–3,8 см обусловлено общей перегородкой и сохранённой ручной толщиной. В санузле первого этажа компромисс глубины около 3,2 см, чтобы не разорвать бойлерную.

## Проверки и допущения

Геометрическая проверка контролирует стеновые объёмы на высоте прохода: они не входят в интерьеры комнат, проёмы лежат внутри стен, торцы соединены, стены не выходят за контур плит. Отдельные проверки сохраняют лестничный проём, 10+9 проступей, площадку, выход наверх, проходы, гараж и скосы. Эти проверки не доказывают сантиметровую точность сканирования.

Неизмеренными остаются плоскости скатов, абсолютная отметка гаража, толщина перекрытия, часть внутренних дверей и конструктивные параметры лестницы. Толщина F2-W06 0,265 м и общая привязка отдельных сканов требуют ручного подтверждения перед строительными работами. Мебель не использовалась.

Подробные решения и изменения: [scan-fit.json](../manual/scan-fit.json). Исторический снимок до подгонки: [scan-fit-before.json](scan-fit-before.json), он не является текущей спецификацией. Единственный источник модели — lib/house.json. Наложения: [overlay.html](overlay.html).
`;
}
